import 'package:flutter/material.dart';

class ProgramattionPage extends StatelessWidget {
  const ProgramattionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: const Text('Mi programación')));
  }
}
